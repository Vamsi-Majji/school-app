{
  "name": "school-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd backend && yarn start\" \"cd frontend && yarn start\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "react-scripts": "^0.0.0"
  },
  "dependencies": {
    "multer": "^2.0.2"
  }
}
